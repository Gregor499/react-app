import {Character} from "../model";
import {render, screen} from "@testing-library/react";
import GalleryItem from "./GalleryItem";
import React from "react";

/*
const testCharacter: Character = {
    //GIVEN
    name: "Herbert",
    image: "testlink",
    status: "aktiv",
    species: "Mensch",
    origin: "",
    loaction: ""
}

test("renders list correctly", () => {
    render(<GalleryItem character={testCharacter}/>)

    expect(screen.getByTestId("testCharacter").textContent).toEqual("Name: Herbert" +
        "Status: aktiv" +
        "Species: Mensch")
})*/
